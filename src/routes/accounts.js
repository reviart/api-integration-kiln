const express = require('express');
// const AccountController = require('../controller/accounts');

const accountRouter = express.Router();

// accountRouter
// .get('/', AccountController.getAccounts)
// .get('/:id', AccountController.getAccountById)
// .get('/:id/portfolio', AccountController.getAccountPortfolio)
// .post('/', AccountController.createAccounts)
// .put('/:id', AccountController.updateAccounts);

module.exports = accountRouter;
